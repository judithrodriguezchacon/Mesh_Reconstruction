# Mesh_Reconstruction

to build the enviroment do:
'''
docker build -t mesh-reconstruction .
'''
to run it:
'''
docker run -it mesh-reconstruction
'''

NOTE:
create all the nodes (python files) inside the src directory under ros2_ws
